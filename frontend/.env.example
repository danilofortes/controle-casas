# URL base da API (FastAPI no Render). Em produção, defina na Vercel.
VITE_API_URL=https://controle-casas-api.onrender.com

# true = preview sem backend (só desenvolvimento de UI)
VITE_USE_MOCK=false
